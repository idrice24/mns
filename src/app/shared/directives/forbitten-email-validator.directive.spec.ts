import { ForbittenEmailValidatorDirective } from './forbitten-email-validator.directive';

describe('ForbittenEmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbittenEmailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});

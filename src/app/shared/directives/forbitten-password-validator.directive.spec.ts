import { ForbittenPasswordValidatorDirective } from './forbitten-password-validator.directive';

describe('ForbittenPasswordValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbittenPasswordValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
